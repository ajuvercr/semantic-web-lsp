searchState.loadedDescShard("derive_more", 0, "What <code>#[derive(AsRef)]</code> generates\nWhat <code>#[derive(AsRef)]</code> generates\nUsing <code>#[derive(Deref)]</code>\nWhat <code>#[derive(DerefMut)]</code> generates\nModule containing macro definitions only, without …\nWhat <code>#[derive(AsRef)]</code> generates\nWhat <code>#[derive(AsRef)]</code> generates\nUsing <code>#[derive(Deref)]</code>\nWhat <code>#[derive(DerefMut)]</code> generates")